#Funcion: Conjunto de codígo que posee parámetros (opcional), instrucciones y resultado(retorno).
#El uso de funciones permite reutilizar código y simplificar nuestros programas

def function(parametros)
    ##codigo
    return resultado

#Importar modulos

import <nombre modulo>

from <nombre modulo> import <elementos>

from <nombre modulo> import <elemento> as <nombre>

#Uso de módulos con primer metodo

modulo.function(argumentos)

#con segundo y tercer método

elemento(argumentos)
nombre(argumentos)

#Funciones deben ser definidas previas a ser invocadas. Si existe más de una llamada a la misma función, Python considera valida la última llamada

#Scope -> variable locales y globales son distintas, no es posible modificar localmente una variable que fue definidad de manera global o en un scope superior, pero si es posible acceder a ellas y utilizarlas.

#Es posible crear modulos con funciones para mayor ordenamiento y su utilización en otros programas mediante la función import.